version: '3.8'
services:
  mosquitto:
    image: eclipse-mosquitto:latest
    container_name: mosquitto_aii
    ports:
      - "1883:1883"
    volumes:
      - ./conf/mosquitto.conf:/mosquitto/config/mosquitto.conf
    restart: unless-stopped
